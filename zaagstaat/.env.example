# URL of the deployed Cloudflare Worker (no trailing slash)
# Leave empty during local development — save/load will silently fail (offline mode)
VITE_WORKER_URL=https://zaagstaat-api.YOUR-SUBDOMAIN.workers.dev
